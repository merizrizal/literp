meta {
  name: Create-UOM
  type: http
  seq: 4
}

post {
  url: http://{{host}}:{{port}}/api/v1/uom
  body: json
  auth: inherit
}

body:json {
  {
    "code": "{{uomCode}}",
    "name": "Each",
    "baseUnit": "each"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
