meta {
  name: Pos-Open-Shift
  type: http
  seq: 42
}

post {
  url: http://{{host}}:{{port}}/api/v1/pos/terminals/{{terminalId}}/shifts
  body: json
  auth: inherit
}

headers {
  Idempotency-Key: {{posOpenShiftIdempotencyKey}}
}

body:json {
  {
    "openingBalance": 250.00,
    "currency": "USD"
  }
}

docs {
  operationId: openPosShift
  request: POST /api/v1/pos/terminals/{terminalId}/shifts
  status: Authenticated; opens one scoped shift for a human principal with an Idempotency-Key.
}

settings {
  encodeUrl: true
  timeout: 0
}
