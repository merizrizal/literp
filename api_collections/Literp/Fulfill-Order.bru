meta {
  name: Fulfill-Order
  type: http
  seq: 30
}

post {
  url: http://{{host}}:{{port}}/api/v1/orders/{{salesOrderId}}/fulfill
  body: json
  auth: inherit
}

body:json {
  {
    "createdBy": "bruno",
    "notes": "Handed over to customer"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
